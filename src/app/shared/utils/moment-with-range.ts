import * as Moment from 'moment';
import * as MomentRange from 'moment-range';

// export const momentWithRange: MomentRange.MomentRange & Moment.Moment = MomentRange.extendMoment(Moment);
export const momentWithRange: MomentRange.MomentRange = MomentRange.extendMoment(Moment);
